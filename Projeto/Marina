from datetime import date
from re import S
from time import sleep

dicionario = {}
lista = []
quartos = [1, 2, 3, 4, 5, 6]
quartosOcup = []

class cadastroHospede():
    def __init__(self):
        pass
    def salvarCadastro(self,cpf,nome):
        dicionario[cpf] = nome 
        lista.append(dicionario)
        return print(lista)
        
    def quartos():
        while quartos == "Ocupado":
            print("Quarto já ocupado")
        else:
            quartos = quartosOcup[:]
            print(quartos)
            print(quartosOcup)
            
    def servico(self, Cafe_manha, Almoco, Lanche, Jantar):
        print("Opções 1 - Café da Manhã, 2- Almoço, 3 - Lanche ou 4 - Jantar ou 0 -  Desistir")
        Cafe_manha =  30
        Almoco =  50
        Lanche = 30
        Jantar = 60
       qt = int(input("Qual quarto deseja solicitar: "))
         
       opcao = int(input("Digite o que deseja: "))
       if opcao == 1:
          print("Café da Manhã R$", Cafe_manha)
       elif opcao == 2:
          print("Almoço R$", Almoco)
       elif opcao == 3:
          print("Lanche R$", Lanche)
       elif opcao == 4:
          print("Jantar R$", Jantar)
       elif opcao == 0:
          print("Desistir")
      else:
          print("")

